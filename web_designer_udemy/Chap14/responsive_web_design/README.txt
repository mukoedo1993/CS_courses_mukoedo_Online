It simply means that the web site responds or adapts to the
size and characteristics of the device that is viewing the page.

e.g.1:
A responsive web site may use a 2-column layout if someone's using a traidtional 
deskop or laptop computer to view the page.
BUT....
What if someone viewed the exact same web page using a small smartphone they
would see a different layout
 --> 1-column layout



Our Responsive Recipe
1: Meta tag in <head> section of HTML
<meta name="viewport">
We need a way to send different CSS instructions to different sized
devices.

2: Media queries (conditional CSS)


Mobile first:
If you want to begin with smartphone, mobile first:
More and more attraction every year...

Mobile first and medium quires both have Many SUPPORTERS.





-------------------------------------------------------
https://www.w3schools.com/css/css_rwd_viewport.asp
<meta name="viewport" content="width=device-width, initial-scale=1.0">
This gives the browser instructions on how to control the page's dimensions and scaling.

The width=device-width part sets the width of the page to follow the screen-width of the device (which will vary depending on the device).

The initial-scale=1.0 part sets the initial zoom level when the page is first loaded by the browser.
48th course
